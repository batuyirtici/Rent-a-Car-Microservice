spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        default-label: master
        git:
          uri: https://github.com/batuyirtici/Rent-A-Car-Microservice-Config-Server
server:
  port: 8888